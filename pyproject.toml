[project]
name = "mistracker"
version = "0.1.0"
description = "A local website to track study mistakes and improve learning"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.0.0",
    "gunicorn>=21.0.0",
]

[project.scripts]
mistracker = "main:main"
mistraker = "main:main"  # legacy alias — misspelling kept for backwards compat
